# Environment
.env
.env.local
.env.*.local
# (.env.example IS committed — it documents required vars with no real values)

# Agent tooling / installed skills (symlinks + local config, not app code)
.agents/
.claude/
skills-lock.json

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Alembic
# (keep alembic/ directory but not __pycache__)

# Node / React
node_modules/
/frontend/build/
/frontend/dist/
.next/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Private planning, memory and AI-assistant files. Named explicitly rather than *.md,
# which also hid every README from a public repo.
CLAUDE.md
**/CLAUDE.md
PLAN.md
plan.md
memory/
.agents/
# Vite's template README, not ours.
frontend/README.md

# graphify knowledge graph (generated, rebuild with: graphify update .)
graphify-out/

# Playwright MCP scratch output (screenshots, snapshots, console logs)
.playwright-mcp/

# Design specs and plans (private working docs)
docs/superpowers/
.superpowers/
